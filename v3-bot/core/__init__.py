"""core/ — new modular feature package (May 27).

For now only `cross_asset_features` lives here; over the next two weeks the
predictor's internals will be split into `core/math.py`, `core/features/`,
and `core/predictor/` per the AUDIT_MAY27 roadmap.
"""
